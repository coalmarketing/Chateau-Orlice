{% extends "layouts/base.njk" %}

{% block body %}
    {% include "sections/landing.njk" %}

    <!-- GDPR -->
    <section class="py-24 ~px-4/8">
        <div class="container m-auto flex justify-center">
            <div class="max-w-[75ch]">
                {% for item in contentOne.items %}
                    <div class="mb-6">
                        {% if item.heading %}
                            <h3 class="~text-xl/2xl font-black mb-4">{{ item.heading }}</h3>
                        {% endif -%}

                        {%- if item.text %}
                            <p class="font-jakarta-sans ~text-sm/base text-gray-800 dark:text-gray-200 mb-2">{{ item.text }}</p>
                        {% endif -%}

                        {%- if item.subitems %}
                            <ul class="list-disc max-w-2xl space-y-1 ml-8">
                                {% for subitem in item.subitems %}
                                    <li class="font-jakarta-sans text-gray-800 dark:text-gray-200">{{ subitem.text }}</li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    </div>
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock body %}